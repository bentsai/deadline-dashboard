[package]
name = "deadline_dashboard"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.41"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
